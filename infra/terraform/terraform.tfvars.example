# コピーして terraform.tfvars にリネームし、値を埋めてください。
# または -var / -var-file で指定してください。

project_id = "your-gcp-project-id"
region     = "asia-northeast1"

# Gemini API Key（Secret Manager に保存されます）。空の場合は後から手動設定。
# gemini_api_key = "your-gemini-api-key"

# イメージは初回 apply 後、ビルド・プッシュしてから設定します。
# cloud_run_api_image = "gcr.io/your-gcp-project-id/circle-api:latest"
